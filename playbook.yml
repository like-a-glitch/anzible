---

- become: true
  connection: local
  hosts: localhost
  roles:
  # start
    - init
  # random
    - appsWithoutSetup
    - git
    - scripts
    - vscodium
    - zsh
  # end
    - autostart
    - keybindings
  vars_files:
    - vars.yml